COMMON_FLAGS="-march=native -O2 -pipe"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"

RUSTFLAGS="${RUSTFLAGS} -C target-cpu=native"

MAKEOPTS="-j8 -l8"
LLVM_TARGETS=""
LLVM_SLOT="22"

#EMERGE_DEFAULT_OPTS="${EMERGE_DEFAULT_OPTS} --with-bdeps n --jobs=12 --load-average=12"
EMERGE_DEFAULT_OPTS="${EMERGE_DEFAULT_OPTS} --with-bdeps n --jobs=1 --load-average=1"

PYTHON_TARGETS="python3_14"
PYTHON_SINGLE_TARGET="python3_14"
LUA_SINGLE_TARGET="lua5-4"
LUA_TARGETS="luajit"
VIDEO_CARDS="nvidia"

# NOTE: This stage was built with the bindist Use flag enabled
# DISTDIR="/var/cache/distfiles"
# PKGDIR="/var/cache/binpkgs"

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
LC_MESSAGES=C.UTF-8

USE="-abi_x86_32 -wayland -ipv6 -X -python -perl -nls curl sqlite readline btrfs -cups -postscript -llvm -initramfs"

GENTOO_MIRRORS="https://gentoo.osuosl.org/"
